{
  "name": "@gfx/array-like-partial-equal",
  "version": "1.0.0",
  "description": "Comparesion of Subarrays of Two Arrays",
  "main": "dist/index.js",
  "scripts": {
    "prepare": "rm -rf dist && npx tsc -p tsconfig.dist.json",
    "prepublishOnly": "npm run test",
    "test": "jest"
  },
  "author": "FUJI Goro",
  "license": "ISC",
  "keywords": ["array", "array-like", "equal"],
  "devDependencies": {
    "@types/jest": "latest",
    "jest": "latest",
    "ts-jest": "latest",
    "ts-node": "latest",
    "typescript": "latest"
  }
}
